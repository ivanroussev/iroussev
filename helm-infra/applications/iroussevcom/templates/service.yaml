apiVersion: v1
kind: Service
metadata:
  name: {{ include "iroussevcom.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "iroussevcom.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 80
