apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "iroussevcom.fullname" . }}
  labels:
    app: {{ include "ivan-portfolio.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "ivan-portfolio.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "ivan-portfolio.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
